@model IEnumerable<Pofo.Models.DepCardPhotos>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Manage/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Department's Cards' Photos</h1>
    </div>

</div>
<div class="row">
    <div class="col-lg-12">

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PhotoName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DepCards.Name)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <input type="image" src="~/Uploads/@item.PhotoName" width="100" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DepCards.Name)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>
    </div>

</div>